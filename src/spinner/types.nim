type
  Orientation* = enum
    Horizontal, Vertical
  SliderRange* = 0.0..1.0

  SliderStyle* = enum
    ## Style options for sliders.
    sliderA, sliderB

  ButtonStyle* = enum
    buttonA, buttonB, buttonC

  ToggleStyle* = enum
    toggleA, toggleB, toggleC

  CheckboxStyle* = enum
    checkboxA, checkboxB

  ProgressStyle* = enum
    progressA, progressB, progressC